{
  "installCommand": "npm install --prefix Backend && npm install --prefix Frontend/demiwal",
  "buildCommand": "npm run build --prefix Frontend/demiwal",
  "functions": {
    "api/[...all].js": {
      "runtime": "@vercel/node"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/[...all].js"
    }
  ]
}